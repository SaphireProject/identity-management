language: java
sudo: false
install: true

jdk:
  - oraclejdk8

script:
  # JaCoCo is used to have code coverage, the agent has to be activated 
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.projectKey=SaphireProject_identity-management -Dsonar.organization=saphireproject -Dsonar.host.url=https://sonarcloud.io  -Dsonar.login=269bea7ab43febfd49f584198d20c83296b0604d

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
